name: "CodeQL Advanced"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    # Запускать каждый вторник в 7:18 UTC
    - cron: '18 7 * * 2'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    timeout-minutes: ${{ (matrix.language == 'swift' && 120) || 360 }}
    permissions:
      # required for all workflows
      security-events: write
      # required to fetch internal or private CodeQL packs
      packages: read
      # only required for workflows in private repositories
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: javascript-typescript
          build-mode: none
        - language: python
          build-mode: none
        # Раскомментируй если есть Kotlin/Java код в Android части
        # - language: java-kotlin
        #   build-mode: autobuild

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Настройка Node.js для TypeScript/JavaScript
    - name: Setup Node.js
      if: matrix.language == 'javascript-typescript'
      uses: actions/setup-node@v4
      with:
        node-version: '22.19.0'
        cache: 'npm'

    # Настройка Python
    - name: Setup Python
      if: matrix.language == 'python'
      uses: actions/setup-python@v5
      with:
        python-version: '3.12.6'
        cache: 'pip'

    # Initializes the CodeQL tools for scanning
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # Дополнительные пакеты запросов для лучшего анализа
        queries: +security-extended,security-and-quality

    # ИСПРАВЛЕННАЯ установка зависимостей для JavaScript/TypeScript
    - name: Install JavaScript dependencies (with error handling)
      if: matrix.language == 'javascript-typescript'
      run: |
        echo "🔧 Setting up JavaScript environment..."
        
        # Проверяем версию Node.js
        node --version
        npm --version
        
        # Очищаем npm кэш
        npm cache clean --force
        
        # Обновляем npm до последней версии
        npm install -g npm@latest
        
        # Проверяем package.json
        if [ ! -f package.json ]; then
          echo "❌ package.json not found!"
          exit 1
        fi
        
        echo "📦 Found package.json, attempting to install dependencies..."
        
        # Сначала пробуем npm ci (если package-lock.json валидный)
        if [ -f package-lock.json ]; then
          echo "📋 Found package-lock.json, trying npm ci..."
          if ! npm ci --no-audit --no-fund; then
            echo "⚠️ npm ci failed, falling back to npm install..."
            rm -f package-lock.json
            npm install --no-audit --no-fund
          fi
        else
          echo "📦 No package-lock.json, using npm install..."
          npm install --no-audit --no-fund
        fi
        
        # Устанавливаем Expo CLI (опционально)
        echo "🚀 Installing Expo CLI..."
        npm install -g @expo/cli || echo "⚠️ Expo CLI installation failed, continuing..."
        
        echo "✅ JavaScript dependencies installed successfully"
      continue-on-error: true

    # Установка зависимостей Python с улучшенной обработкой ошибок
    - name: Install Python dependencies (with error handling)
      if: matrix.language == 'python'
      run: |
        echo "🐍 Setting up Python environment..."
        
        # Проверяем версию Python
        python --version
        pip --version
        
        # Обновляем pip
        python -m pip install --upgrade pip setuptools wheel
        
        # Переходим в backend директорию
        if [ -d "backend" ]; then
          cd backend
          echo "📁 Found backend directory"
          
          # Проверяем requirements.txt
          if [ -f requirements.txt ]; then
            echo "📋 Found requirements.txt, installing dependencies..."
            
            # Показываем содержимое для отладки
            echo "📄 Requirements.txt contents:"
            head -10 requirements.txt
            
            # Устанавливаем зависимости с таймаутом
            pip install -r requirements.txt --timeout=100 --retries=3
          else
            echo "⚠️ requirements.txt not found in backend directory"
            echo "📂 Backend directory contents:"
            ls -la
          fi
        else
          echo "⚠️ Backend directory not found"
          echo "📂 Root directory contents:"
          ls -la
        fi
        
        echo "✅ Python setup completed"
      continue-on-error: true

    # Настройка переменных окружения для Django
    - name: Setup Django environment
      if: matrix.language == 'python'
      run: |
        if [ -d "backend" ]; then
          cd backend
          echo "🔧 Setting up Django environment variables..."
          echo "SECRET_KEY=test-secret-key-for-codeql-analysis-$(date +%s)" > .env
          echo "DEBUG=False" >> .env
          echo "ALLOWED_HOSTS=localhost,127.0.0.1" >> .env
          echo "DB_ENGINE=django.db.backends.sqlite3" >> .env
          echo "DB_NAME=:memory:" >> .env
          echo "✅ Django environment configured"
        fi
      continue-on-error: true

    # Manual build step (если понадобится)
    - if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo 'Manual build mode detected but not configured'
        echo 'Add your build commands here if needed'
        exit 1

    # Выполнение CodeQL анализа
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"

    # Проверка зависимостей на известные уязвимости (только если установка прошла успешно)
    - name: Run security checks
      if: success() || failure()
      run: |
        if [ "${{ matrix.language }}" == "javascript-typescript" ]; then
          echo "🔍 Running npm security audit..."
          if [ -f package.json ]; then
            npm audit --audit-level=moderate --omit=dev || echo "⚠️ npm audit found issues"
          fi
        elif [ "${{ matrix.language }}" == "python" ]; then
          echo "🔍 Running Python security checks..."
          if [ -d "backend" ]; then
            cd backend
            pip install safety || echo "⚠️ Could not install safety"
            safety check --ignore=70612 || echo "⚠️ Safety check found issues"
          fi
        fi
      continue-on-error: true

    # Финальный отчет
    - name: Analysis Summary
      if: always()
      run: |
        echo "=== CodeQL Analysis Summary ==="
        echo "🎯 Language: ${{ matrix.language }}"
        echo "✅ CodeQL analysis completed"
        echo "📊 Results will be available in GitHub Security tab"
